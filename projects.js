function btnPrContent() {
    // Simulate generating HTML content dynamically
    var btnContent = "<a>+New Project</a>";
    return btnContent;
}
function generateContent() {
    // Simulate generating HTML content dynamically
    var htmlContent = "<p>This is the updated content from projects.js.</p>";
    return htmlContent;
}
