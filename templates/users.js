function btnPrContent() {
    // Simulate generating HTML content dynamically
    var btnContent = "<a>+Create User</a>";
    return btnContent;
}
function generateContent() {
    // Simulate generating HTML content dynamically
    var htmlContent = "<p>This is the updated content from users.js.</p>";
    return htmlContent;
}
