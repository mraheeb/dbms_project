function btnPrContent() {
    // Simulate generating HTML content dynamically
    var btnContent = "<a>+Create Tasks</a>";
    return btnContent;
}
function generateContent() {
    // Simulate generating HTML content dynamically
    var htmlContent = "<p>This is the updated content from tasks.js.</p>";
    return htmlContent;
}
